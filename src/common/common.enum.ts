/**
 * Specifies the direction for ordering results.
 *
 * @enum {string}
 * @property {OrderDirection.ASC} ASC - Ascending order.
 * @property {OrderDirection.DESC} DESC - Descending order.
 */
export enum OrderDirection {
  ASC = 'ASC',
  DESC = 'DESC'
}
